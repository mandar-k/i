play.application.loader = com.livelygig.product.keeper.impl.KeeperApplicationLoader

//cassandra-journal.keyspace=usersApi
//cassandra-snapshot-store.keyspace=usersApi
//lagom.persistence.read-side.cassandra.keyspace=usersApi
akka.actor.provider = "akka.cluster.ClusterActorRefProvider"

authentication {
  tokenExpire = 1 day
}

activation {
  tokenExpire = 3 day
}